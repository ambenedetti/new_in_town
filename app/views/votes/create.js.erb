var containers = document.querySelectorAll(".tip_votes_<%= @tip.id %>");

var votesPanel = "<%= escape_javascript(render 'components/votes', user_votes: @user_votes, tip: @tip) %>";

containers.forEach(function(container) {
  container.innerHTML = votesPanel;
});
